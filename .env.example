# Configurações do Servidor
PORT=3000
HOST=localhost
NODE_ENV=development

# URL base da API (para produção)
BASE_URL=https://sua-api.com

# Origens permitidas para CORS (separadas por vírgula)
# Exemplo: ALLOWED_ORIGINS=https://seu-frontend.com,https://app.seu-dominio.com
ALLOWED_ORIGINS=

# Configurações de Banco de Dados (se aplicável)
# DATABASE_URL=mongodb://localhost:27017/hotel-management

# Configurações de Autenticação (se aplicável)
# JWT_SECRET=your-super-secret-key
# JWT_EXPIRES_IN=7d